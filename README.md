# space-station-api

API REST pública (sin autenticación) para el ejercicio combinado de Trailhead:
Workshop Battle Station + Use External Services with a Flow.

## Local

```bash
npm install
npm run dev
```

URLs:

- http://localhost:3000/health
- http://localhost:3000/station
- http://localhost:3000/schema
